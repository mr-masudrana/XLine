<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Needed so the SIP UDP registration can be kept alive with minimal throttling -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".SipApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.SipCaller">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <activity
            android:name=".MyAccountActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".HelpActivity"
            android:exported="false" />

        <activity
            android:name=".AboutActivity"
            android:exported="false" />

        <activity
            android:name=".CallActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <!-- Foreground service keeps the UDP SIP registration alive while the
             app is backgrounded. UDP has no server-push mechanism like
             TCP/TLS providers often support, so this service is mandatory
             for reliably receiving incoming calls.

             specialUse (not phoneCall) is used deliberately: phoneCall type
             is meant for apps integrated with Android's Telecom/
             ConnectionService framework, which this app doesn't use. Calling
             it that without the integration is a policy mismatch that can
             get the service killed by the OS. specialUse is the documented
             fit for "keep a background network connection alive" work that
             doesn't match a standard category. -->
        <service
            android:name=".SipCallService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
        </service>

    </application>
</manifest>
